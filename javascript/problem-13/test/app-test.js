var should = require('should');
var app = require('../lib/app');

describe('problem 13', function() {

  // it('should have an array of data to play with', function() {
  //   should.exist(app.numbers);
  // });

  describe('first test', function() {
    //empty
  });
});